#include "teseus.hpp"

  void teseus::buscar(const laberint & M, const posicio & inici, const posicio & final, std::list<posicio> & L) throw(error){
  std::cout<<inici.first<<final.first;
  laberint l;
  l = M;
  std::list<posicio> a;
  L = a;
  }
